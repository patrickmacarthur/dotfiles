# .inputrc
#
# Author: Patrick MacArthur <patrick@patrickmacarthur.net>
#
# A basic inputrc.

# Turn off the annoying system beep in bash and other readline-based
# programs.
set bell-style none

# I prefer vi to emacs, so I set this.  Emacs mode is default, you can
# explicitly set it with "set editing-mode emacs".  You can also define
# custom keybindings in either mode.
set editing-mode vi

# Bind insert key to quoted-insert, since that sort of makes sense
"\e[2~": quoted-insert

# vim: ft=readline:
